# Database connection string for Azure SQL Server
DATABASE_URL=mssql+pyodbc://username:password@gracefellowshipserver.database.windows.net/GraceFellowshipDB?driver=ODBC+Driver+18+for+SQL+Server

# JWT Secret key (generate a strong random string)
SECRET_KEY=your-super-secret-key-change-this-in-production

# Encryption key for PCO credentials (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_KEY=your-fernet-encryption-key

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:5173,https://pco-viz-frontend.fly.dev

# Trial duration in days
TRIAL_DURATION_DAYS=14
